<h1>New Question</h1>
<%= form_for [:user, @question] do |f| %>
  <%= render "form", f: f %>
  <div class="form-group">
    <%= f.submit t(".submit"), class: "form-control btn btn-primary" %>
  </div>
<% end %>

<div class="row">
  <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="col-md-10 col-md-offset-1">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="/"><%= t ".top_lawyer" %></a>
        </div>
        <ul class="nav navbar-nav">
          <li><%= link_to t(".articles"), root_path %></li>
          <li><%= link_to t(".questions"), root_path %></li>
        </ul>

        <%= render "layouts/shared/top_search" %>

        <% if current_account.nil? %>
          <%= render "layouts/shared/login_buttons" %>
        <% else %>
          <%= render "layouts/shared/account_buttons" %>
        <% end %>
      </div>
    </div>
  </nav>
</div>
<div class="row cover_image">
</div>

<div class="col-md-6 col-md-offset-3">
  <h3><%= t ".edit_law_profile" %></h3>
  <%= form_for [:lawyer, @lawyer_profile] do |f| %>
    <%= render "form", f: f %>

    <div class="form-group"> 
      <%= f.submit t(".submit"), class: "form-control btn btn-primary" %>
    </div>
  <% end %>
</div>
